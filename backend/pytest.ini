[pytest]
asyncio_mode = auto
markers =
    live: интеграционные тесты к реальному Ozon API требуют ключей, для запуска: pytest -m live
# Live-тесты исключены из дефолтного прогона (CI должен быть детерминированным):
# запускать явно через pytest -m live.
addopts = -m "not live"